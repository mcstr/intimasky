<div class="container-mask pt-5 text-center">
  <h1>What do you desire?</h1>
  <% if @masks %>
    <div class="row justify-content-center">
      <% @masks.each do |mask| %>
        <div class="card-product m-4 col-lg-2 col-md-4 col-sm-12">
          <div class="border-card">
            <div class="card__image-container">
            <%= cl_image_tag mask.photo.key%>
            </div>
          </div>

          <div class="card__content">
            <h5><%= link_to(mask.name, mask_path(mask)) %></h3>
            <h6><%= mask.description %></h5>
            <p>€<%= mask.price %> per day</p>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>
  <a class="button" href="">back to masks</a>
  <a class="submit-button" href="">Submit</a>
</div>



<!-- if statement because of Heroku -->


